<%- include('partials/header') %>

<section class="dashboard">
    <div class="container">
        <h2 class="dashboard-title">Resumes</h2>
        <div class="resume-grid">
            <div class="resume-card new-resume" onclick="window.location.href='/resume/new'">
                <span class="plus-icon">+</span>
                <p>Create a new resume</p>
            </div>

            <% resumes.forEach(resume => { %>
                <div class="resume-card">
                    <div class="resume-content">
                        <h3><%= resume.name %></h3>
                        <p>Last updated: <%= resume.updatedAt.toDateString() %></p>
                    </div>
                    <button class="options-btn" onclick="toggleDropdown('<%= resume.id %>')">⋮</button>
                    <ul class="dropdown-menu" id="dropdown-<%= resume.id %>">
                        <li><a href="/resume/preview/<%= resume.id %>">Open</a></li>
                        <li><a href="/resume/edit/<%= resume.id %>">Edit</a></li>
                        <li><a href="#" onclick="deleteResume('<%= resume.id %>')">Delete</a></li>
                    </ul>
                </div>
            <% }) %>
        </div>
    </div>
</section>

<%- include('partials/footer') %>
